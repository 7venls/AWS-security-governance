# SSH

SSH = 遠隔でサーバの中に入る安全な通路

・SSh = サーバーに遠隔でログインする仕組み。

・EC2は画面（GUI）がない ⇒ 基本はSSH操作。

・鍵（キーペア）で本人確認するため、ID/パスワードより安全。

イメージ：鍵（pem）を持っている人だけが、２２番ポートの玄関からサーバーに入れる。

### なぜブラウザじゃなくSSH？

EC2はサーバだから

・画面を触る前提じゃない

・24時間動く

・自動処理が多い

人が操作する前提じゃない世界

### 鍵認証とは？
